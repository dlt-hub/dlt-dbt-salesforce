/* Table: lead */
{{
    config(
        materialized='incremental'
    )
}}
SELECT
    t.id,
    t.is_deleted,
    t.last_name,
    t.first_name,
    t.salutation,
    t.name,
    t.title,
    t.company,
    t.street,
    t.city,
    t.state,
    t.postal_code,
    t.country,
    t.phone,
    t.fax,
    t.email,
    t.photo_url,
    t.lead_source,
    t.status,
    t.industry,
    t.rating,
    t.annual_revenue,
    t.owner_id,
    t.is_converted,
    t.is_unread_by_owner,
    t.created_date,
    t.created_by_id,
    t.last_modified_date,
    t.last_modified_by_id,
    t.system_modstamp,
    t.clean_status,
    t.is_priority_record,
    t.sic_code__c,
    t.product_interest__c,
    t.primary__c,
    t.current_generators__c,
    t.numberof_locations__c,
    t._dlt_load_id,
    t._dlt_id,
    t.mobile_phone,
    t.number_of_employees,
    t.last_viewed_date,
    t.last_referenced_date,
FROM  {{ ref('stg_lead') }} as t